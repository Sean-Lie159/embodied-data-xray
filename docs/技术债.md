# 技术债清单

> 记录项目中已知但暂不处理的非阻塞问题，避免 lint 报错沦为背景噪音
> （"破窗效应"：旧报错淹没新错误）。处理时须单独提交，不与功能改动混入。

## 1. `app/agent/agent.py` 既有 lint 报错清理

- **状态**：待处理（排在 generate_report 前后处理即可，不紧急）
- **问题**：基于 pyright 严格检查的既有报错，多为历史遗留，非本轮引入：
  - `MaxTurnsExceeded` 分支的返回类型不匹配（`tuple[str, list[Any], None]`，line ~105）。
  - `RunContextWrapper` / `Any` 显式类型（`reportExplicitAny` / `reportAny`，line 11 / 44 / 66 / 68 / 87 / 102 / 109 等）。
  - `SYSTEM_PROMPT` 隐式字符串拼接（`reportImplicitStringConcatenation`）。
- **影响**：不会导致运行错误，但作为背景噪音存在，可能淹没后续新引入的问题。
- **处理建议**：给 `run_turn` 的返回值用更精确的联合类型（如 `None` 分支显式标注）、
  收紧 `Any` 使用（优先 `Tool` / `RunResultItem` 等具体类型）、为多行字符串拼接改用
  括号或三引号。处理完应使该文件 lint 归零。
- **优先级**：低（不影响功能，仅影响可维护性与可观测性）。
